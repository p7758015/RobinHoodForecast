# Runtime data (canonical)

- `football_agent.db` — SQLite: прогнозы, результаты матчей, outcomes
- `snapshots/` — снимки нормализованных данных (v2, пока пустой каркас)

Файлы БД и снимки **не коммитятся** (см. `.gitignore`).
В репозитории остаётся только `.gitkeep` / этот README.
